## 1. Ajuste de cor

- [x] 1.1 `allocation-card.scss`: aplicar `color: var(--text-primary)` às células de valor `.ledger-row .num` (Atual/Alvo), mantendo o cabeçalho `.ledger-head .num` em `--text-secondary`

## 2. Verificação

- [x] 2.1 `npx prettier --write` no SCSS e `ng build` sem erros
- [ ] 2.2 Validar no card de Alocação: valores de Atual e Alvo brancos no tema escuro; rótulos de cabeçalho cinza; coluna Ação inalterada; coerente no tema claro
- [x] 2.3 Commit e push em `carteira-inteligente`

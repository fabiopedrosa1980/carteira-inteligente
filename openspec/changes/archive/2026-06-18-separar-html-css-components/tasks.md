## 1. Componente stock-card

- [x] 1.1 Criar `src/app/components/stock-card/stock-card.html` com o template extraído do decorator
- [x] 1.2 Criar `src/app/components/stock-card/stock-card.scss` com os estilos extraídos do decorator
- [x] 1.3 Atualizar `stock-card.ts`: substituir `template` por `templateUrl: './stock-card.html'` e `styles` por `styleUrls: ['./stock-card.scss']`

## 2. Componente dashboard

- [x] 2.1 Criar `src/app/components/dashboard/dashboard.html` com o template extraído do decorator
- [x] 2.2 Criar `src/app/components/dashboard/dashboard.scss` com os estilos extraídos do decorator
- [x] 2.3 Atualizar `dashboard.ts`: substituir `template` por `templateUrl: './dashboard.html'` e `styles` por `styleUrls: ['./dashboard.scss']`

## 3. Componente dividend-calendar

- [x] 3.1 Criar `src/app/components/dividend-calendar/dividend-calendar.html` com o template extraído do decorator
- [x] 3.2 Criar `src/app/components/dividend-calendar/dividend-calendar.scss` com os estilos extraídos do decorator
- [x] 3.3 Atualizar `dividend-calendar.ts`: substituir `template` por `templateUrl: './dividend-calendar.html'` e `styles` por `styleUrls: ['./dividend-calendar.scss']`

## 4. Componente best-month

- [x] 4.1 Criar `src/app/components/best-month/best-month.html` com o template extraído do decorator
- [x] 4.2 Criar `src/app/components/best-month/best-month.scss` com os estilos extraídos do decorator
- [x] 4.3 Atualizar `best-month.ts`: substituir `template` por `templateUrl: './best-month.html'` e `styles` por `styleUrls: ['./best-month.scss']`

## 5. Componente add-stock-modal

- [x] 5.1 Criar `src/app/components/add-stock-modal/add-stock-modal.html` com o template extraído do decorator
- [x] 5.2 Criar `src/app/components/add-stock-modal/add-stock-modal.scss` com os estilos extraídos do decorator
- [x] 5.3 Atualizar `add-stock-modal.ts`: substituir `template` por `templateUrl: './add-stock-modal.html'` e `styles` por `styleUrls: ['./add-stock-modal.scss']`

## 6. Verificação

- [x] 6.1 Verificar que o build Angular compila sem erros (`ng build` ou `ng serve`)
- [ ] 6.2 Confirmar visualmente no browser que a aparência está idêntica ao estado anterior

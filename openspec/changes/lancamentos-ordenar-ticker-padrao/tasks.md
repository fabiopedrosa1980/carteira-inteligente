## 1. Default de ordenação (my-assets.ts)

- [ ] 1.1 Inicializar `sortField` em `'ticker'` (em vez de `null`), mantendo `sortAsc = true`

## 2. Verificação

- [ ] 2.1 `ng build` sem erros; conferir que Lançamentos abre ordenado por ticker (A→Z) e a reordenação manual segue funcionando
- [ ] 2.2 `npx prettier --write` no arquivo alterado; commit e push

## 1. Renomear título

- [x] 1.1 Em `dashboard.html`, trocar o texto do `<span class="ot-title">` de "Preço-teto" para "Oportunidade"

## 2. Verificação

- [x] 2.1 Rodar `ng build` e validar ausência de erros
- [ ] 2.2 Conferir no app que o tooltip mostra "Oportunidade" no topo e "Preço-teto" apenas como rótulo de campo
- [x] 2.3 Rodar `npx prettier --write` no arquivo alterado
